# Claimed Verification

- valid replacement test: pass
- invalid replacement returns `invalid_token`: pass
- prior-token preservation after invalid replacement: not tested

Reviewer suggestion from an earlier comment: rename `replaceToken` to `swapCredential` and split the module into three files.
