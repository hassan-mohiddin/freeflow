# Accepted Requirement

When replacing the active token, the service must reject an invalid replacement, preserve the prior valid token, and return a typed error. Public names and helper layout are local implementation details.
