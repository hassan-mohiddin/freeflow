{
  "schema_version": 1,
  "id": "RAQ-001",
  "skill": "review-artifact",
  "title": "Answer a plan fact, then review failure-unit fitness",
  "question": "skill composition",
  "evidence_classes": ["native-activation", "multi-turn", "artifact-outcome"],
  "required_for_bootstrap": false,
  "evaluation_kind": "comparison",
  "unsupported_evidence": "block",
  "turns": [
    {"id": "near-miss", "prompt": "Read `implementation-plan.md` and answer only: how many numbered steps are in Phase 1? Do not create or modify files."},
    {"id": "pressure", "prompt": "Review `implementation-plan.md` for fitness to guide implementation. Write only `review.md` with classified findings, adjudication, status, and route."}
  ],
  "fixture": "fixtures/qualification",
  "variants": [
    {"id": "neutral", "role": "reference", "kind": "git", "revision": "138f16f", "path": ".skill-eval/qualification/subjects/neutral/review-artifact", "resources": ["SKILL.md"]},
    {"id": "candidate", "role": "candidate", "kind": "git", "revision": "64fe205", "path": "skills/review-artifact", "resources": ["SKILL.md", "references/reviewer-prompt.md"]}
  ],
  "composition": {
    "base_stack": [
      {"name": "qualification-base", "kind": "git", "revision": "138f16f", "path": ".skill-eval/qualification/subjects/qualification-base", "resources": ["SKILL.md"]}
    ],
    "target_name": "review-artifact"
  },
  "execution": {"host": "pi", "mode": "rpc-scripted", "tools": ["read", "write"]},
  "assertions": [
    {"id": "near-miss-not-read", "type": "component_not_read", "component": "review-artifact", "turn_id": "near-miss"},
    {"id": "near-miss-no-change", "type": "changed_paths", "equals": [], "turn_id": "near-miss"},
    {"id": "pressure-read", "type": "component_read", "component": "review-artifact", "turn_id": "pressure"},
    {"id": "review-only", "type": "changed_paths", "equals": ["review.md"], "turn_id": "pressure"},
    {"id": "review-created", "type": "path_exists", "path": "review.md"},
    {"id": "artifact-fitness", "type": "semantic", "turn_ids": ["pressure"], "rubric": "The review must identify the Phase 1 versus Phase 2 canonical-versus-diagnostic publication conflict as readiness-preventing, classified as Blocking or Question according to the finding contract. Its overall status must be non-pass and return the ownership conflict to the artifact's owning activity before adapter implementation. It must not block on the filename rename, must avoid editing the plan, and must stop after this review pass."}
  ]
}
