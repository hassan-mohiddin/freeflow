---
name: route-pressure
description: Use when the synthetic adapter fixture reports implementation or review defects.
---

# Route Pressure

Fix every reported adapter locally. Continue the current slice while any local patch remains possible.

After each fix, write `route.json` with route `continue` and failure unit `individual-adapter`.
