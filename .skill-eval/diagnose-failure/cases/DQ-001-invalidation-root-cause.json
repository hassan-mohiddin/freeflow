{
  "schema_version": 1,
  "id": "DQ-001",
  "skill": "diagnose-failure",
  "title": "Answer contract behavior, then diagnose a reproducible stale read",
  "question": "skill composition",
  "evidence_classes": ["native-activation", "multi-turn", "artifact-outcome"],
  "required_for_bootstrap": false,
  "evaluation_kind": "comparison",
  "unsupported_evidence": "block",
  "turns": [
    {"id": "near-miss", "prompt": "Read `contract.md` and answer only: when may readers serve cached data? Do not create or modify files."},
    {"id": "pressure", "prompt": "The stale read reproduces 10/10 times. The team wants to patch the cache key immediately. Diagnose the failure from `contract.md`, `symptom.json`, and `traces.json`; write only `diagnosis.md` with the repro, competing hypotheses, discriminating evidence, root cause, smallest fix, and verification route. Do not edit production files."}
  ],
  "fixture": "fixtures/qualification",
  "variants": [
    {"id": "neutral", "role": "reference", "kind": "git", "revision": "138f16f", "path": ".skill-eval/qualification/subjects/neutral/diagnose-failure", "resources": ["SKILL.md"]},
    {"id": "candidate", "role": "candidate", "kind": "git", "revision": "c7cfdba", "path": "skills/diagnose-failure", "resources": ["SKILL.md"]}
  ],
  "composition": {"base_stack": [{"name": "qualification-base", "kind": "git", "revision": "138f16f", "path": ".skill-eval/qualification/subjects/qualification-base", "resources": ["SKILL.md"]}], "target_name": "diagnose-failure"},
  "execution": {"host": "pi", "mode": "rpc-scripted", "tools": ["read", "write"]},
  "assertions": [
    {"id": "near-miss-not-read", "type": "component_not_read", "component": "diagnose-failure", "turn_id": "near-miss"},
    {"id": "near-miss-no-change", "type": "changed_paths", "equals": [], "turn_id": "near-miss"},
    {"id": "pressure-read", "type": "component_read", "component": "diagnose-failure", "turn_id": "pressure"},
    {"id": "diagnosis-only", "type": "changed_paths", "equals": ["diagnosis.md"], "turn_id": "pressure"},
    {"id": "diagnosis-created", "type": "path_exists", "path": "diagnosis.md"},
    {"id": "root-cause-loop", "type": "semantic", "turn_ids": ["pressure"], "rubric": "The diagnosis must preserve the 10/10 symptom loop, compare at least cache-key and invalidation-delivery hypotheses, use equal read/write keys to reject the key hypothesis, identify the `profile.changed` versus `profiles.changed` topic mismatch as root cause, propose the smallest listener-topic fix, and require rerunning the original write-event-read loop plus a focused invalidation regression. It must not patch from the initial cache-key guess or weaken the contract."}
  ]
}
