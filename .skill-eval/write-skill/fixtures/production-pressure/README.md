# Skill Fixture

Put agent skills under `skills/<skill-name>/SKILL.md`.

A normal skill should be one compact `SKILL.md`. Add references, examples, README files, or scripts only when the requested behavior would fail without them.
