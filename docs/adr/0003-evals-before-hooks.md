# Evals Before Hooks

Freeflow should prove behavior with baseline versus with-skill evals before adding enforcement hooks. Context-loading runtime may load the canonical compact kernel and independently enabled capability instructions, but hooks that block or enforce behavior can come later only after measured failures justify them.
