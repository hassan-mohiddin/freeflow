# Evals Before Hooks

Freeflow should prove behavior with baseline versus with-skill evals before adding enforcement hooks. Context-loading hooks may load already-authored workflow instructions, but hooks that block or enforce behavior can come later only after measured failures justify them.
