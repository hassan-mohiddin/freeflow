# Freeflow v0.7.2 Release Evidence

## Status and identity

- **Status:** Prepared release candidate; the immutable release tag, npm publication, GitHub Release, and consumer verification remain later human-controlled boundaries.
- **Package:** `@hassangameryt/freeflow`
- **Version:** `0.7.2`
- **Preparation date:** 2026-09-11
- **Source implementation checkpoint:** `d7114b7f76e68dc95f704d6a511a8d9556a15143` (`Merge pull request #3 from hassan-mohiddin/fix/changelog-release-eof`)
- **Release preparation:** `npm run release:prepare -- 0.7.2 --date 2026-09-11` updates the package, lockfile, root and host manifests, Claude marketplace metadata, and changelog.
- **Version basis:** The user explicitly selected `0.7.2` for the experimental Cognitive Routing surface despite the current Unreleased section labeling the routing changes as breaking. This records the selected target; it is not a universal compatibility claim.
- **Final release identity:** The immutable `v0.7.2` tag and its exact release commit remain pending final identity review.
- **Release notes:** [`CHANGELOG.md#0.7.2---2026-09-11`](../../CHANGELOG.md#072---2026-09-11)

The candidate keeps one canonical portable skill tree and the same package boundary across host metadata and delivery adapters. It does not create a second runtime tree or claim that unqualified host/model behavior is universally supported.

## Consumer-visible changes

- Replaces the experimental Cognitive Routing switch/boundary protocol with Coordinator/Executor assignments, separate delegate/return/unit/projection tools, and `projection`/`thinking` configuration. Old experimental configuration and tools are not migrated.
- Consolidates experimental routing reads into `freeflow_project inspect` with scoped candidates and `freeflow_unit inspect` with current/history/detail views; earlier experimental operation names are no longer advertised or accepted.
- Adds exact assistant-visible-text evidence refs, bounded saved-work detail/history recovery, snapshot-based inspection pagination, and explicit target limitations in expanded routing output.
- Preserves routing communication, source attribution, delivered-user identity across compaction, saved reports, explicit resume, evidence selection, and streamed operation receipts while keeping normal inspection bounded.
- Retires the previous experimental routing implementation into the historical archive and updates the shipped skills, runtime prompts, Pi extension, generated output, public documentation, and release notes.

## Checks and observations

- Release preparation dry run passed and listed exactly the nine documented version-bearing paths: seven JSON manifests, `package-lock.json`, and `CHANGELOG.md`.
- Non-dry release preparation passed on a clean `release/0.7.2` worktree and changed only those documented paths.
- `npm ci --ignore-scripts` passed with 139 packages added and zero reported vulnerabilities.
- `npm run build` passed, and `git diff --exit-code -- pi-extension/dist` confirmed generated runtime cleanliness.
- `npm run check` passed, including type checking, source and host-manifest validation, changelog and documentation checks, formatting, activation/runtime checks, version consistency, command surface and package boundary checks.
- `npm run check:release-metadata` passed in prepublish mode. It verified manifest consistency, the canonical 24-skill host surface, host adapters, release-boundary docs, package cleanliness, and deferred install smoke.
- Focused release suites passed: Pi extension 179/179, host manifests and adapters 8/8, Skill Author 19/19, Skill Eval 80/80, Track Work 24/24, snapshot 4/4, release utilities 7/7, release workflow 4/4, changelog 12/12, and documentation 5/5.
- `npm run release:notes -- --version 0.7.2` produced a non-empty 3,218-byte release-note file.
- Exact `npm pack --ignore-scripts` inspection passed: `hassangameryt-freeflow-0.7.2.tgz`, 227 files, packed size 5,472,566 bytes, unpacked size 6,862,541 bytes, shasum `a3e1a0f60a9f726e5f17172cd1eb1940bb4be903`, and integrity `sha512-xbR333zFuGnFA8La3xN2M3k7F9vUx4DBvLeU1mfOlpuqwpPw/Q83PBjfIQdYyXNfZhS3B9LPrdq6px9VizU//Q==`. Package inspection found no GitHub-only/deprecated entries, included the required runtime entrypoints, and excluded `opencode.json` as required.
- `git diff --check` passed.

The npm tarball is the installable runtime artifact. GitHub-only documentation, task memory, evaluation definitions, and deprecated historical material remain outside the runtime package. The package retains one shared runtime hook and five host adapters for the supported delivery paths.

## Deferred and unavailable evidence

- Live Claude smoke evals remain deferred.
- GitHub/Codex, Claude, Gemini, Cursor, Copilot/VS Code, Kiro, OpenCode, Hermes, and fresh Pi installation smoke checks are not established by local metadata validation.
- Native host behavior, host trust UI, registry propagation, npm provenance, marketplace review, and consumer-side installation require external systems and remain unverified until their respective boundaries.
- Deterministic checks do not establish universal model behavior, skill readiness, or compatibility with every host version.
- The immutable tag/source identity, npm publication, GitHub Release, and consumer verification remain pending final review and explicit authorization.

Freeflow uses one shared runtime hook. Freeflow uses host-native skill invocation instead of duplicate manifest command handlers. Live Claude smoke evals remain deferred; these statements preserve the release-boundary evidence required for later publication verification.
