# Freeflow v0.7.1 Release Evidence

## Status and identity

- **Status:** Release candidate prepared from the reviewed multi-host distribution change; the immutable release tag and external publication remain later human-controlled boundaries at this evidence-capture point.
- **Package:** `@hassangameryt/freeflow`
- **Version:** `0.7.1`
- **Preparation date:** 2026-09-04
- **Source implementation checkpoint:** `a99860a37317a184a7cd5b32f7737d559b994355` (`feat(distribution): add multi-host plugin surfaces`)
- **Release preparation:** `npm run release:prepare -- 0.7.1` updates the package, lockfile, root and host manifests, Claude marketplace metadata, and changelog.
- **Final release identity:** the immutable `v0.7.1` tag and its exact release commit remain the later human-controlled release boundary.
- **Release notes:** [`CHANGELOG.md#0.7.1---2026-09-04`](../../CHANGELOG.md#071---2026-09-04)

The candidate keeps one canonical portable skill tree and adds host delivery metadata and adapters over that source. It does not duplicate the 25 skills into host-specific directories, and it does not turn the Pi-specific capabilities or Pi extension into a general host runtime.

## Consumer-visible changes

- Adds Agent Plugins 1.0, Gemini CLI, Cursor, GitHub Copilot/VS Code, OpenCode v2, and Hermes distribution metadata or compatibility over the same canonical 25-skill tree, with Kiro compatibility through the portable Agent Plugins skill surface.
- Separates shared runtime-context rendering from Codex, Claude, Gemini, Cursor, and Copilot/VS Code hook adapters while preserving existing Pi/PiFlow capability boundaries and lifecycle behavior.
- Aligns the OpenCode v2 skill configuration, Gemini lifecycle matching, and Copilot session-start context output with their documented host contracts.
- Keeps host-native skill invocation instead of duplicate manifest command handlers. Freeflow uses host-native skill invocation instead of duplicate manifest command handlers.

## Checks and observations

- `npm run check` — passed at 0.7.1, including type checking, source and host-manifest validation, changelog and documentation checks, formatting, activation/runtime checks, and the 221-file package boundary.
- `npm run check:release-metadata` — passed in prepublish mode. It verified manifest consistency, the canonical 25-skill host surface, host adapters, release-boundary docs, and package cleanliness. Install smoke remains deferred.
- Focused release suites passed: Pi extension 194/194, host manifests and adapters 8/8, Skill Author 19/19, Skill Eval 80/80, Track Work 24/24, snapshot 4/4, release utilities 7/7, release workflow 4/4, changelog 11/11, and documentation 5/5.
- `npm run release:notes -- --version 0.7.1` produced a non-empty 628-byte release-note file.
- Exact `npm pack --ignore-scripts` inspection passed with isolated npm cache: `hassangameryt-freeflow-0.7.1.tgz`, 221 files, packed size 5,425,092 bytes, unpacked size 6,846,392 bytes, shasum `81a3145399627d400ebaf982c6a27184cdc8e9c9`, and integrity `sha512-mKP4Pcs99DfVEX5zHPjvPMVBb7z7uzw/pyqWWVuHysQAlsIqIcDQLVqkAL/1dZPWD6Pma2ZeWWgUpInDxB3A0g==`.
- `git diff --check` passed. The host's default npm cache contains root-owned files, so npm checks and packaging used an isolated cache; no source or package defect was observed from that host condition.

The npm tarball is the installable runtime artifact. GitHub-only documentation, task memory, evaluation definitions, and deprecated historical material remain outside the runtime package. The package contains one shared runtime hook and five host adapters for the changed Codex, Claude, Gemini, Cursor, and Copilot/VS Code delivery paths.

## Deferred and unavailable evidence

- Live Claude smoke evals remain deferred.
- GitHub/Codex, Claude, Gemini, Cursor, Copilot/VS Code, Kiro, OpenCode, Hermes, and fresh Pi installation smoke checks are not run by local metadata validation.
- Native host behavior, host trust UI, registry propagation, provenance, marketplace review, and consumer-side installation require external systems and remain unverified until their respective publication or smoke-check boundary.
- The locally installed OpenCode version is older than the documented v2 configuration surface, so an OpenCode native smoke check is unavailable in this environment; the repository-only `opencode.json` still follows the current documented array shape.
- Deterministic checks do not establish universal model behavior, skill readiness, or compatibility with every host version.

Freeflow uses one shared runtime hook. Freeflow uses host-native skill invocation instead of duplicate manifest command handlers. Live Claude smoke evals remain deferred; these statements preserve the release-boundary evidence required for later publication verification.
