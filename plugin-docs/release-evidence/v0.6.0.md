# Freeflow v0.6.0 Candidate Release Evidence

## Status and identity

- **Status:** Prepared local candidate; not committed, tagged, published, or released in this boundary.
- **Package:** `@hassangameryt/freeflow`
- **Version:** `0.6.0`
- **Preparation date:** 2026-08-25
- **Source checkpoint before version promotion:** `3580da9` (`feat(release): complete v0.6 groundwork`)
- **Final release identity:** the immutable `v0.6.0` tag and its exact source commit remain a later human-controlled boundary.
- **Release notes:** [`CHANGELOG.md#0.6.0---2026-08-25`](../../CHANGELOG.md#060---2026-08-25)

The candidate promotes the verified Unreleased work without changing the released v0.5.0 section. The release-preparation command updated package, marketplace, manifest, lockfile, and changelog version state; it did not commit or create remote state.

## Consumer-visible changes

- Establishes the canonical `plugin-docs/` public documentation hub and archives historical project documentation under `.deprecated/project-docs/`.
- Removes the legacy Pi `/discover` and `/execute-plan` compatibility aliases; use `/discuss` and `/execute-work`.
- Clarifies Workflow and Cognitive Routing current-context skill reads, including Track Work before record operations.
- Documents PiFlow Cognitive Routing keyboard shortcuts: `Ctrl+Shift+R` for manual holds and `Ctrl+Shift+A` for automatic profile cycling.
- Hardens changelog governance and tag-driven release CI with pre-publication release-note checks, exact artifact verification, and recovery safeguards.

The removed aliases are a consumer-visible compatibility change. The current supported command surface and migration guidance are recorded in [ADR 0010](../adr/0010-remove-pi-compatibility-aliases.md).

## Checks and observations

Observed candidate evidence:

- `npm run check` — passed typecheck, source, canonical changelog, workflow invariants, docs, formatting, release-version, command, activation, runtime, and package checks.
- `npm run test:pi-extension` — passed all 192 Pi extension tests.
- CI-equivalent deterministic tests passed: 19 Skill Author tests, 80 Skill Eval tests, 66 Track Work tests, 4 snapshot tests, 7 release utility tests, 4 release workflow tests, 11 changelog tests, and 5 docs tests.
- `npm run check:release-metadata` — passed release identity, package boundary, host metadata, and deferred smoke checks.
- `npm pack --dry-run --ignore-scripts` — produced 198 files as `hassangameryt-freeflow-0.6.0.tgz`; shasum `4019e7255dc16d9cffaff84afcfd8f7625b73dd2`; integrity `sha512-7Zu88wBDthuSdyeDXOiHz6J9vUoA7CSpdNlByrV/gCVzxCejv2nVauesmcTedFNRAcgR/jlG6LbB1XvL0rI2mQ==`.
- `node scripts/release-notes.mjs --version 0.6.0` — extracted a non-empty release-note section successfully.
- Release preparation dry run and actual promotion both passed; the release tool now preserves repository package-lock formatting.

## Command surface

The candidate registry contains:

- 4 mode commands.
- 18 direct skill calls.
- 3 developer skill calls.
- 2 Pi native settings commands.

## Runtime and package boundary

- The npm tarball is the installable runtime artifact.
- GitHub-only `plugin-docs/`, `.skill-eval/`, and `.deprecated/` material remain outside the npm package.
- Freeflow uses host-native skill invocation instead of duplicate manifest command handlers.
- The package contains one shared runtime hook and one Pi extension entrypoint.
- Generated Pi output must remain clean after the build.
- Release CI publishes one exact `npm pack` tarball with npm provenance, then verifies registry checksum and provenance before creating the GitHub Release.

## Deferred and unavailable evidence

- Live Claude smoke evals remain deferred.
- GitHub/Codex, Claude, and fresh Pi install smoke checks are not run by local validation.
- Remote GitHub installation, host trust UI, registry propagation, and consumer installation remain unverified until publication.
- npm Trusted Publishing and the protected GitHub `npm` environment are external prerequisites and cannot be established by this local candidate.
- Deterministic checks do not establish universal model behavior, skill readiness, or Cognitive Routing quality.
