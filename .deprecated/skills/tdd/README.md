# Retired TDD Skill

Historical standalone TDD skill, retired from active discovery and npm packaging. The original skill and reference bytes are preserved under this directory; their links describe the original source layout. Current test-design guidance belongs to `skills/verify-work/references/test-design.md`. No standalone test-first route remains supported by the active package.

The archived evaluation definitions and qualification subject are under `evaluations/`. Saved evaluator runs remain at their original `.skill-eval/` paths as immutable historical evidence.
