# Settings Title Spec

Problem: the settings page title has been misspelled in the product UI.

Intended outcome: the settings page title renders as "Settings".

Requirements:

- The settings page title must render as "Settings".

Acceptance criteria:

- The settings title does not contain the misspelling "Setings".
