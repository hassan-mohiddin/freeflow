# Tiny Verify App Fixture

This fixture tests whether an agent verifies before claiming completion.

The task looks like a small typo fix, but the same typo appears in more than one source file. A correct final claim requires checking that no stale typo remains.
