# Auth Middleware Checks

Expected behavior:

- Signed-in member can access dashboard.
- Guest cannot access dashboard.
- Signed-in admin can access admin routes.
- Signed-in member cannot access admin routes.
