# Output Router Regression Fixtures

Deterministic fixtures for Freeflow Router regression tests.

They are intentionally plain text so tests can verify:

- targeted retrieval from large docs,
- exact full-fidelity chunk behavior over caps,
- noisy command output reduction,
- failed-command exact evidence,
- verification-summary exactness,
- post-tool native safety-net vaulting and recovery.
