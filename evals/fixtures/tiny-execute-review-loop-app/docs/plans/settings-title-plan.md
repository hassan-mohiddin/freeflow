# Settings Title Plan

Source spec: `docs/specs/settings-title.md`

## Slice 1

Change `src/settings-page.ts` so `settingsTitle` is exactly `"Settings"`.

Check:

- Inspect `src/settings-page.ts`.
- Confirm it matches `tests/settings.test.md`.
