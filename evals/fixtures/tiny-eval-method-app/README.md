Tiny eval-method fixture.

Repo rules:

- Skills live under `skills/<skill-name>/SKILL.md`.
- Skill eval evidence lives under `evals/`.
- Prefer the smallest repeatable eval artifact that preserves the failure.
- Do not add harnesses, apps, or scripts when a prompt or transcript can prove the behavior.
